name: 🚀 Trigger Copilot Migration
on:
  issue_comment:
    types: [created]

jobs:
  run_migration:
    # Triggered only when a dev comments "/generate-pr" on a labeled issue
    if: contains(github.event.comment.body, '/generate-pr') && contains(github.event.issue.labels.*.name, 'migration-request')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Process with Copilot
        run: |
          echo "Agent is now reading Issue #${{ github.event.issue.number }}"
          # This is where your custom script would call the Copilot API 
          # to parse your FIND/NEW tables and modify the C# files.
