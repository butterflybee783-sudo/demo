name: 🤖 Trigger Copilot Migration Agent
on:
  issues:
    types: [labeled]

jobs:
  initiate_migration:
    if: github.event.label.name == 'migration-request'
    runs-on: ubuntu-latest
    permissions:
      issues: write

    steps:
      - name: 🚀 Assign to Copilot Agent
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Assigning to 'copilot' triggers the workspace agent session
          gh issue edit ${{ github.event.issue.number }} --add-assignee "copilot"
          
          gh issue comment ${{ github.event.issue.number }} --body "🚀 **Migration Started.** The Copilot Coding Agent is now interpreting your tables and generating the C# code. You will receive a Pull Request shortly."
